import 'package:flutter/material.dart';


class Bookdelivery extends StatelessWidget {
  const Bookdelivery({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Book Delivery'),centerTitle: true,
      backgroundColor: Colors.yellow,),
      );

    
  }
}